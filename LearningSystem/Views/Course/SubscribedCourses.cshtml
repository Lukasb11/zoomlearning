@model IEnumerable<LearningSystem.Models.course>

@{
    ViewBag.Title = "List of courses";
}

<h2>List of my subscribed courses</h2>

<table class="table">
    <tr>
        <th>
            Course name
        </th>
        <th>
            Course description
        </th>
        <th>
            Course length
        </th>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.course_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.course_desc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.course_length) minutes
            </td>
            <td>
                @Html.ActionLink("Details", "Details", new { id = item.course_id }) |
                @Html.ActionLink("Unsubscribe", "Unsubscribe", new { id = item.course_id }) 
            </td>
        </tr>
    }
</table>
@if (Model.Count() == 0)
{
    <H2>
        No courses found!
    </H2>
}